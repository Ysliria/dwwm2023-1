{% extends 'base.html.twig' %}

{% block title %}Formulaire d'ajout{% endblock %}

{% block body %}
    <h1>Ajouter une formation</h1>

    <hr>

    {{ form_start(formation_add) }}
        {{ form_widget(formation_add) }}

        <button class="btn btn-primary col-2 offset-10">Ajouter</button>
    {{ form_end(formation_add) }}
{% endblock %}

